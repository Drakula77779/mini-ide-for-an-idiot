# Mini IDE (C++)

Run this script in a terminal:

```bash
chmod +x ide.sh
./ide.sh
```

Inside the menu:
- `1` edits `main.cpp`
- `2` builds with `g++`
- `3` runs the compiled program
- `4` builds and runs
